# SVG Track Styler

A lightweight web tool to apply double-stroke styles to SVG GPX tracks directly in your browser.

## 🌐 Live Demo
Once published on GitHub Pages, it will be available at:
`https://twproject.github.io/pages-svg-track-styler/`

## ⚙️ Features
- Load and edit `.svg` files locally
- Customize **outer/inner stroke colors and width**
- Remove watermark text like "created by gps..."
- Built-in **Help** overlay (❔ button)
- 100% client-side — no uploads

## 🧩 How to Use
1. Open the web app
2. Upload your `.svg` map file
3. Choose colors, stroke widths, and group ID (default `trk1`)
4. (Optional) check "Remove watermark text"
5. Click **Generate styled SVG** and download your file

## 🎨 Color Input
You can use color names (`red`, `yellow`, `green`) or hex codes (`#ff0000`, `#00ff00`).

## 🚀 Deployment on GitHub Pages
1. Create a new repository (or reuse your existing one): `pages-svg-track-styler`
2. Upload the following files:
   - `index.html`
   - `style.css`
   - `script.js`
   - `README.md`
3. Go to **Settings → Pages → Source = main / (root)**
4. Your web app will be live at `https://<yourusername>.github.io/pages-svg-track-styler/`

## 📜 License
MIT License — Free to use and modify.
